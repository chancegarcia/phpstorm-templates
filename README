In order to just parse the license into a new file, I'm using

#if(${Module})#end
#parse("license.php")

The file is needing the custom variable 'Module', so in the base file, I prompt for it and that value is passed to the license for parsing.
